const express = require('express');
const router = express.Router();
const authIngresoController = require('../controller/authIngresoController');

router.post('/api', authIngresoController);

module.exports = router;